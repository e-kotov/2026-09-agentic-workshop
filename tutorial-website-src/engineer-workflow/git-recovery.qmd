---
title: "Git and recoverable change"
---

Git makes an agent's edits inspectable and recoverable. Read `git diff` before accepting work. Commit useful checkpoints with a clear message. A commit does not prove the work is correct.

```bash
git status --short
git diff -- path/to/file
git log --oneline --max-count=5
```

Lab 2 breaks and repairs a disposable project. Keep deliberate failures isolated, record the starting commit, and never experiment in a production checkout. If you do not want a change, stop the agent, inspect the diff, and use the smallest recovery operation your team accepts.

::: {.callout-warning}
Do not commit credentials, restricted data, or provider configuration. Review staged paths and `.gitignore`. An ignored file is not securely protected.
:::
