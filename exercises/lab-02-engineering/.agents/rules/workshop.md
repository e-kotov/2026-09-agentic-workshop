# Antigravity project rule

@../../AGENTS.md

This file uses the current `.agents/rules/` workspace-rule location and `@` import syntax. Verify activation mode in the installed Antigravity version; this rule is guidance, not isolation.
