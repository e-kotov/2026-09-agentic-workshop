# Lab 1 project instructions

Work only within this lab and its `work/` directory unless the participant explicitly expands scope. Use fake/source-free data only; never read or create credentials, restricted rows, unpublished results, or personal/participant data. Preserve `task/` and `checks/` inputs. Ask before network access or destructive operations. Show diffs and run the focused check before claiming completion. Linux R package installation uses the repository-scoped skill in `.agents/skills/r-binary-packages/SKILL.md`; do not install arbitrary packages for this exercise.
