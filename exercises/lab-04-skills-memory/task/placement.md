# Placement exercise

Repeated task: validate a repaired release, recompute manifest counts and SHA-256 values, and produce the income summary.

Classify it before implementing as a one-off prompt, project/global instruction, skill, memory/log, or deterministic script. Explain why deterministic code is the smallest correct home for byte-level transformations, how the checker provides evidence, and what a skill would not guarantee. Skill creation is optional.
