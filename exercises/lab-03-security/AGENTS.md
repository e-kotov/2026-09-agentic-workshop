# Lab 3 project instructions

Stay inside this lab and its disposable `work/` directory.

- Use only the immutable invented fixtures. Never read, create, request, or paste credentials, restricted rows, unpublished results, personal data, host files, or external paths.
- At the first interaction, ask the participant to choose Standalone or optional Cumulative and wait. If they do not choose, default to Standalone.
- During orientation do not run task commands or edit files. Confirm the lab-root command, expected `pwd` suffix, README route, read-only `fixture/`, `checks/`, and `tools/`, writable `work/`, and exact checker (`python checks/check_lab3.py`).
- After the route is chosen, follow the README's Beginner order and its one-shot/continuation prompt. Do not start optional routes or Lab 4 before the mandatory spine passes.
- Standalone is complete by itself; cumulative export is optional and follows a passing check plus human review.
- Keep the stale checksum as evidence, preserve source hashes, label runtime observations versus static evidence, and record limitations.
- Rules, permissions, prompts, and redaction filters are incomplete controls; do not claim OS isolation. Stop after the checker prints both success lines and a human has reviewed the artefacts.
