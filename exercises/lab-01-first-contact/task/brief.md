# Bounded task brief

Inspect only `task/metadata.csv`. Produce `work/corrected-metadata.csv` with the documented value label corrected, preserve the input, and write one sentence of provenance. Verify with `Rscript checks/check_metadata.R`. If the intended correction is ambiguous, stop and ask rather than inventing a codebook.
