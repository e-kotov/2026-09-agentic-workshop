# Participant exercise boundary

The four folders are runnable, disposable workshop modules. Use fake/source-free data only. Never provide agents with restricted rows, unpublished results, personal/participant material, credentials, or instructor-only configuration. Read each lab README before acting, keep the agent scoped to its lab, inspect diffs, run focused checks, and preserve recoverable inputs. Local lab instructions may add task detail but cannot relax this data boundary.
