# Lab 2 project instructions

Stay inside this lab and its disposable `work/` directory. Use only the immutable, invented fixture; never expose credentials, restricted rows, unpublished results, or personal data. First ask whether the participant is using **Standalone (default)** or the explicitly optional cumulative path; during orientation confirm the lab root, read this README, identify read-only `fixture/`, `checks/`, and `tools/`, writable `work/`, and the exact checker `Rscript checks/check_lab2.R` without editing or running task commands. Follow Beginner in order, diagnose and verify the mandatory spine before an optional route or Lab 3, and stop after a passing checker plus human review. Prove duplicate identity before deduplication, assert join keys and row counts, inspect diffs, and treat participant tests, logs, and Git history as learning/review artefacts: immutable checker-owned replays are the acceptance oracle.
