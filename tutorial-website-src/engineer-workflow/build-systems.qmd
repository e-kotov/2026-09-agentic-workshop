---
title: "Build systems and reproducibility"
---

A workflow system records dependencies and rebuilds only the affected outputs. It makes execution order explicit, but it cannot review your research decisions.

- In R, [`targets`](https://books.ropensci.org/targets/) describes objects and dependencies and rebuilds only what is stale.
- In Python or cross-language work, [Snakemake](https://snakemake.readthedocs.io/) expresses rules, inputs, outputs, and environments.

Record the environment and inputs in a lockfile or manifest. Fix random seeds where appropriate. For a release, record provenance and checksums. Make, Drake, renv, uv, or CI may also fit. Choose a system your team can maintain.

::: {.callout-tip}
### Try it now (8 minutes)

Sketch three nodes: input, cleaned table, and report. Ask an agent which changes should invalidate each node. Compare its graph with the files and checks in Lab 2.
:::
