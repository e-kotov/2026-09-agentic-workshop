# Lab 4 project instructions

Stay inside this lab's disposable `work/`; use only the immutable invented fixture. Never expose credentials, restricted rows, unpublished results, personal data, or instructor configuration. First ask whether the participant is using **Standalone (default)** or the explicitly optional cumulative path; during orientation confirm the lab root, read this README, identify read-only `fixture/`, `checks/`, `tools/`, and shipped skills, writable `work/`, and the exact checker `Rscript checks/check_lab4.R` without editing or running task commands. Follow Beginner in order, diagnose and classify before optional skill creation, follow the two-attempt loop, stop after a pass or failed attempt 2 and human review, and do not jump to later work. Put deterministic transformations in code, inspect diffs, and treat memory/logs as oversight aids rather than correctness proof.
