---
title: "Memory and agentic loops"
---

Use project memory to record decisions and handoffs. Keep it small enough to maintain. A useful structure has the goals and current state, a work log, decision records, a handoff, and an `analysis/` folder with `data/`, clean code, tests, and outputs.

Bound every agentic loop: plan, act, check, revise, then stop. Add an explicit stop condition and a human review point. Before adopting a memory system, check its usefulness, maintenance cost, duplication, stale state, context bloat, and inappropriate automation.

Use a handoff to cross a real task boundary: continue while the current task and context remain coherent, then start fresh when the handoff can replace irrelevant session history. A warm provider cache can make continuation cheaper, but it should not override clarity or recoverability.

Conductor-for-all is one comparison, not the prescribed system. Do not automate research decisions before you can review them. Logs should clarify human responsibility. The minimal control-project template in Lab 4 remains unpublished until it passes an agent privacy and structure audit and Emil's manual review.
