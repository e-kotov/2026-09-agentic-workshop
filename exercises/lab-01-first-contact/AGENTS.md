# Lab 1 instructions

Use `README.md` as the task source. First ask the participant to choose standalone (default) or cumulative. If they do not choose, stay standalone. During orientation, confirm the current directory, name the immutable `fixture/release/` defect, writable `work/` folder, and exact checker `Rscript checks/check_lab1.R`; do not edit or run task commands yet.

Follow **Beginner — recommended** in order. Do not jump to optional routes, later labs, or the cumulative project. Do not suggest completing every route. Stop at the README's check/review condition.

Read `fixture/`, `task/`, `checks/`, and `tools/` only as needed; edit only `work/`. Preserve the ambiguous metadata and stop if duplicate rows disagree; package work is dry-diagnostic only. Use only supplied source-free/fake data. Never request, read, create, or transmit credentials, restricted rows, unpublished results, private instructor material, or personal/participant data. Do not use network access, `sudo`, destructive Git commands, or host paths.

Before claiming completion, run `Rscript checks/check_lab1.R`, report its exact result, and ask the participant to review the three artefacts. Cumulative export is allowed only after a standalone pass and explicit participant approval, and only through `tools/handoff.sh` to `../workshop-project/lab-01/`.
